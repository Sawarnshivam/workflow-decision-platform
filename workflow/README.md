# Workflow Layer

This folder is reserved for workflow definitions.

Currently workflow logic is implemented in:

engine/workflow_engine.py

and configured through:

config/workflow_config.json

Future workflows (e.g., loan approval, claim processing) can be added here.